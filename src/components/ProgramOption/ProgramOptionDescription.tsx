import React from "react";

const ProgramOptionDescription = ({ description }: { description: string }) => (
  <p className="pb-1 font-light">{description}</p>
);

export default ProgramOptionDescription;
